# Local configuration for build/publish scripts. Copy to `.env` (gitignored) and fill in.
#
# S3-compatible upload target for `scripts/build-windows.sh --upload`.
# Credentials themselves live in ~/.s3cfg (never in the repo).
S3_BUCKET=s3://your-bucket
S3_PUBLIC=https://your-bucket.s3.your-region.example.com
